[build]
builder = "nixpacks"

[deploy]
# start.sh checks $RAILWAY_SERVICE_NAME and runs the correct command:
#   web service    → streamlit run app.py ...
#   cache-warmer   → python precompute.py
startCommand = "bash start.sh"
restartPolicyType = "on_failure"
